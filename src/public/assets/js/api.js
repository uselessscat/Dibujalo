class Api {
    static loadImageRandom() {
        /*
        $.ajax({
            url: 'get-image',
            data: '1=1',
            type: 'POST',
            async: false,
            dataType: 'json',
            success: function (resp) {
                if (resp.type === 'ok') {
                    $('#imgDibujo').prop('src', resp.ruta);
                    $('#imgDibujo').prop('alt', resp.nombre);
                    $('#imgDibujo').prop('title', resp.nombre);
                    $('#imgDibujo').prop('data-id', resp.id);
                }
            }
        });
        */
    }
}